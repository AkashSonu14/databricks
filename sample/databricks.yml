bundle:
  name: sample
  uuid: 1d025062-8d73-4c4b-8d85-9328861dffd6

include:
  - resources/*.yml

sync:
  include:
    - src/**
  
# artifacts:
#   python_artifact:
#     type: whl
#     build: uv build --wheel

variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-b41210d2-2c92.cloud.databricks.com
      profile: dev
    variables:
      catalog: workspace
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: https://dbc-b41210d2-2c92.cloud.databricks.com
      profile: prod
      root_path: /Workspace/Users/akasha14122001@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: workspace
      schema: prod
    permissions:
      - user_name: akasha14122001@gmail.com
        level: CAN_MANAGE
